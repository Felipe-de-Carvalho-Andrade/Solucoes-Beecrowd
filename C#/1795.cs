using System;
using System.Globalization;

class URI {
    static void Main(string[] args) {
        int r = int.Parse(Console.ReadLine());

        long resultado = (long)Math.Pow(3, r);

        Console.WriteLine(resultado);
    }
}